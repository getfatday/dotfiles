---
# Ansible role requirements for dotfiles repository
roles:
  - name: ansible-role-dotmodules
    src: git+https://github.com/getfatday/ansible-role-dotmodules.git

collections:
  - name: geerlingguy.mac
    version: ">=1.0.0"
