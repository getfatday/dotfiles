---
# Google Workspace CLI (gws)
# https://github.com/googleworkspace/cli

homebrew_packages:
  - googleworkspace-cli    # Google Workspace CLI

homebrew_casks:
  - google-cloud-sdk       # gcloud CLI (required for gws auth setup)
