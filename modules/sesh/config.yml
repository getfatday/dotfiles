---
# sesh — smart tmux session manager
# Provides fuzzy session switching, directory-based sessions via zoxide

homebrew_packages:
  - gum                   # TUI components (alternative sesh picker)
  - sesh                  # Tmux session manager
  - zoxide                # Smart cd replacement (replaces z and autojump)

stow_dirs:
  - sesh

mergeable_files:
  - ".zshrc"
