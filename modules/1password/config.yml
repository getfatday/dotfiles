---
# 1Password module
# Homebrew Cask application with SSH agent configuration

homebrew_casks:
  - 1password
  - 1password-cli                 # `op` CLI for secret references and env injection

stow_dirs:
  - 1password
