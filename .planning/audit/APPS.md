# App Config Audit

## Needs New Module: Karabiner

**Config:** 109KB `karabiner.json` at `/Volumes/Macintosh HD-1/Users/ianderson/.config/karabiner/`

6 complex modification rules:
1. Emacs Selection
2. Emacs key bindings [option+keys] (rev 5)
3. Emacs key bindings [control+keys] (rev 10)
4. Emacs key bindings [C-x key strokes] (rev 2)
5. HHKB Pro Emacs
6. HHKB Pro Navigation

Plus: caps_lock -> left_control, 12 device overrides, 12 function key remaps.

**Action:** Create `modules/karabiner/` with config.yml + karabiner.json.

## Needs New Module: Rust

**Found:** `.cargo/` directory with bin/, env/, registry/. No custom config.toml.

Both `.zshenv` and `.profile` source `$HOME/.cargo/env`.

**Action:** Create `modules/rust/` that:
- Declares `rust` formula (already in old Cellar)
- Provides `.zshenv` fragment: `. "$HOME/.cargo/env"`

## No Module Needed: SSH

No `~/.ssh/config` existed on old laptop. Only a `known_hosts` file.
The old laptop used 1Password SSH agent exclusively.

**Action:** No SSH module needed. SSH agent config is handled by 1Password module.

## No Module Needed: Rectangle

Not found on old laptop.

## Existing but Not in Dotfiles: Claude Code Settings

`~/.claude/settings.json` on old laptop had:
- MCP servers: openclaw-comms, smith-tools
- Plugins: ralph-loop, taches-cc-resources
- Hooks: gsd-check-update (SessionStart)
- Status line: gsd-statusline

These are runtime/project-specific configs, not suitable for dotfiles.

## Existing but Not in Dotfiles: OpenClaw

`~/.openclaw/` directory with logs/, tasks/, workspace/. Runtime data, not config.

## Existing but Not in Dotfiles: gh (GitHub CLI)

`~/.config/gh/config.yml` had `git_protocol: https` and alias `co: pr checkout`.
Also had `hosts.yml` with `github.expedia.biz` (corporate — exclude).

**Action:** Consider adding `~/.config/gh/config.yml` to git module. Exclude hosts.yml.

## Existing: Misc .config/ Items

| Directory | Status |
|-----------|--------|
| asdf-direnv | Auto-generated by asdf |
| direnv/lib/use_asdf.sh | Auto-generated |
| karabiner | **Needs module** (see above) |
| gh | Partially useful (config.yml only) |
| git/ignore | Has `.claude/settings.local.json` — add to gitignore |
| egctl.yaml | **Corporate** — Expedia partner tool with API credentials |
| hillia | Corporate project tracking |
| okta-auth | **Corporate + credentials** — cached Okta session tokens |
| op | 1Password config — runtime, skip |
| schema-deploy-auth | Corporate |
| iterm2 | Covered by iterm module |

## Global .tool-versions (asdf)

```
nodejs 25.2.1
jq 1.7.1
```

**Action:** Add to node module or create asdf module.

## .npmrc

Points to Expedia Artifactory with auth tokens. **Corporate + secrets. DO NOT migrate.**

## .sops.yaml

```yaml
creation_rules:
  - pgp: C146F99D7C6AFEB0A330CBDF4B39021F936EAE3C
    encrypted_regex: '^PG.*$'
    filepath_regex: '.*\.env$'
```

Personal SOPS config for encrypting .env files. Could go in a `sops` or `security` module if still using SOPS.
